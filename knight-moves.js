/**
 * you need to think about the rules of the board and knight,
 * and remember to follow them.
 *
 *
 * answer => the rule of the board would be a road for the knight to take
 *        => the rule of the knight is to like an L
 */

/**
 * for every square there is a number of possible moves,
 * don't make a move that can go over the board.
 *
 * array = [ ];
 * **/

/**
 * decide what search algorithm to use, remember that
 * one of them could be potentially infinite series
 */

/**
 * use the search algorithm and output every path it would take
 */
